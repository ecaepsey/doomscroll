
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
      POSTGRES_DB: doomscroll
    ports:
      - "5432:5432"
    volumes:
      - doomscroll_pg:/var/lib/postgresql/data
volumes:
  doomscroll_pg: